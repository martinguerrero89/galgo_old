##libraries

require(parallel) #For parallelization of individual solutions
require(doParallel)
require(cba) #for subset of matrix distance
require(cluster)
require(survival)
require(gplots)
require(nsga2R) #For nsgaII
require(gpuR) #For GPU usage
require(matchingR) #For solving gale-shapley algorithm (stable marriage problem)
require(Biobase)
require(caret)
require(illuminaio) #Read BGX files for illumina chips
require(survcomp)
require(genefu)

require(survminer)
require(MetaGxBreast)
require(GEOquery)
require(a4Base)
require(gplots)
require(xlsx)
require(SummarizedExperiment)
require(TCGAbiolinks)
require(limma)
require(edgeR)
require(hgu133plus2.db)
require(RColorBrewer)
